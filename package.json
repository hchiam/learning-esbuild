{
  "dependencies": {
    "esbuild": "^0.8.31",
    "react": "^17.0.1",
    "react-dom": "^17.0.1"
  },
  "scripts": {
    "build": "esbuild app.jsx --bundle --outfile=out.js --define:process.env.NODE_ENV=\\\"production\\\""
  },
  "browser": {
    "path": "path-browserify"
  }
}
